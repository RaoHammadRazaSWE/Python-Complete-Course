# Random Password Generater:
import random
import string

# print(string.ascii_letters)

# print(string.ascii_lowercase)

# print(string.digits)

# print(string.punctuation)

# randomNum = random.choice([1,2,3])
# print(randomNum)

# val = random.choice(['a', 'b', 'c', 'd'])
# print(val)

# Generate Password Random: Method 1.

# pass_len = 8
# charValues = string.ascii_letters + string.digits + string.punctuation + string.ascii_lowercase

# password = ""
# for i in range(pass_len):
#     password += random.choice(charValues)

# print("Your Random Password is:", password)

# Method 2:
# Using  List comprehension [functon for i in range(n)] Generate Password

pass_len = 8
charValues = string.ascii_letters + string.digits + string.punctuation + string.ascii_lowercase


# res = [random.choice(charValues) for i in range(pass_len)]

# IF you want res concatenate any number of string:

password = "".join ([random.choice(charValues) for i in range(pass_len)])

print(password)
